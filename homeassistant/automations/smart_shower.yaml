# Smart Shower Automation

- alias: "Shower Mode: Start"
  trigger:
    - platform: state
      entity_id: binary_sensor.shower_in_use
      to: 'on'
  action:
    - service: light.turn_on
      target:
        entity_id: light.bathroom_lights
      data:
        brightness_pct: 80
        color_temp_kelvin: 3333 # Soft white
    - service: script.turn_on
      entity_id: script.shower_music
    - service: notify.level_info
      data:
        message: "Shower mode activated. Enjoy!"

- alias: "Shower Mode: End"
  trigger:
    - platform: state
      entity_id: binary_sensor.shower_in_use
      from: 'on'
      to: 'off'
  action:
    - service: media_player.media_stop
      target:
        entity_id: media_player.bathroom
    - service: light.turn_on
      target:
        entity_id: light.bathroom_lights
      data:
        brightness_pct: 100
        color_temp_kelvin: 9000 # Back to bright cleaning light
    - service: script.turn_on
      entity_id: script.set_bathroom_lights_to_default
